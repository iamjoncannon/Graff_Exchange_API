// const { spawnSync, spawn } = require("child_process")

// spawnSync("createdb test", {shell: true})

// console.log('created test database')

// spawn("redis-server", {shell: true})

// spawn("redis-cli -c 'flushall'", {shell: true})

// console.log("launched redis")
